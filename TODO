* add tests for Z.Query to make sure a model doesn't get added twice
* add tests for Z.Object.extended
* add tests for observers getting removed during didChangeProperty

* Z.Array.sortBy
* finish Z.cmp
* fire option doesn't work that well with array @ observers
* make Z.Array and Z.Hash Orderable
* remove is<Type> properties, use Z.Object.isA instead
* implement named finders
* figure out model serialization
  * something RABL-like?
  * does this belong in the framework?
* batched property changes
* docs
