* separate out utils
  * isArray
  * isFunction
  * isString
  * isNumber
  * isNaN
  * isBoolean
  * isRegExp
  * isNull
  * isUndefined
* hash functions for all types
* implement a more robust Z.eq method that does deep comparisons of native
  objects
* implement better toString method
* Z.Hash
* hash functions for Z.Array and Z.Hash
* docs
