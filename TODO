* implement Z.Array#getUnknownProperty
* factor out code that handles path parsing (splitting)
* allow for read-only properties (set `set` property to false? use `readonly` option?)
* add properties to toString
